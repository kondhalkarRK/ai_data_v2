Could not load KPIs.

KPI query failed against the analytics warehouse. Confirm migrate + seed for this industry, then retry. (ProgrammingError: (psycopg.errors.AmbiguousParameter) could not determine data type of parameter $3 LINE 14: AND ($3 IS NULL OR c.make = $3) ^ [SQL: SELECT SUM(f.total_sales) AS revenue, SUM(f.order_qty) AS units_sold, COUNT(DISTINCT f.order_id) AS total_orders, COUNT(DISTINCT f.region_id) AS active_regions, SUM(f.total_sales) / NULLIF(COUNT(DISTINCT f.order_id), 0) AS avg_order_value, SUM(f.total_sales) / NULLIF(SUM(f.order_qty), 0) AS rev_per_unit FROM automotive.fact_sales f JOIN automotive.dim_carline c ON c.carline_id = f.carline_id JOIN automotive.dim_region r ON r.region_id = f.region_id WHERE (%(start_date)s IS NULL OR f.sales_date >= %(start_date)s) AND (%(end_date)s IS NULL OR f.sales_date <= %(end_date)s) AND (%(make)s IS NULL OR c.make = %(make)s) AND (%(region)s IS NULL OR r.region_name = %(region)s) ] [parameters: {'start_date': datetime.date(2026, 1, 1), 'end_date': datetime.date(2026, 9, 11), 'make': None, 'region': None}] (Background on this error at: https://sqlalche.me/e/20/f405))

1) Confirm API is running (`http://localhost:8000/health`). 2) Open the app at http://localhost:3000. 3) Restart both with .\scripts\dev.ps1 after env changes. 4) If needed: python scripts\migrate.py automotive upgrade head + seed, then refresh.
